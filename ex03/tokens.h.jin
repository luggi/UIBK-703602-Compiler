/* Authors:
 * - Huber Lukas
 * - Alexander Hirsch
 * - Patrick Ober
 * - Michael Tscholl
 * - Franz Josef Haider
 */

#ifndef TOKENS_H
#define TOKENS_H

enum token {
    {% for name in TOKENS -%}
    {{name}},
    {% endfor %}
};

/* convert given token to string */
const char *token_string(const enum token t);

#endif
